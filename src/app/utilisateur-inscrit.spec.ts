import { UtilisateurInscrit } from './utilisateur-inscrit';

describe('UtilisateurInscrit', () => {
  it('should create an instance', () => {
    expect(new UtilisateurInscrit()).toBeTruthy();
  });
});
